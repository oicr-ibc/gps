#!/bin/sh
# postinst script for gps
#
# see: dh_installdeb(1)

set -e

. /usr/share/debconf/confmodule

. /usr/share/dbconfig-common/dpkg/postinst 
dbc_generate_include='template:/etc/gps/gps-config.groovy'
dbc_generate_include_perms="755"
dbc_generate_include_owner="root:root"
dbc_generate_include_args="-o template_infile=/etc/gps/gps-config.groovy.template -U"
dbc_go gps $@

# summary of how this script can be called:
#        * <postinst> `configure' <most-recently-configured-version>
#        * <old-postinst> `abort-upgrade' <new version>
#        * <conflictor's-postinst> `abort-remove' `in-favour' <package>
#          <new-version>
#        * <postinst> `abort-remove'
#        * <deconfigured's-postinst> `abort-deconfigure' `in-favour'
#          <failed-install-package> <version> `removing'
#          <conflicting-package> <version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package


case "$1" in
    configure)

    	RET=""
        db_get gps-server/admin_password

        if [ "$RET" != "" ] ; then
            echo Updating administrator password.
            # We should really hash the password instead of storing in clear-text
            #adminpw=`echo -n $RET | sha256sum | cut -d ' ' -f 1`
            adminpw=$RET
            tfile=`mktemp`
	        if [ ! -f "$tfile" ]; then
               return 1
            fi
			cat /etc/gps/shiro.ini | sed \
			  -e "s/^administrator\s*=.*,/administrator=$adminpw,/" \
			  > $tfile && mv $tfile /etc/gps/shiro.ini
        fi

    	# Create gps user if it doesn't exist.
        if ! id gps > /dev/null 2>&1 ; then
            adduser --system --home /var/lib/gps --no-create-home \
				 --disabled-password gps
        fi

        rm -f /usr/share/gps
        ln -s /usr/share/gps-* /usr/share/gps

        if [ ! -e /var/lib/gps/conf ] ; then
          ln -s /etc/gps /var/lib/gps/conf
        fi

        chown -R gps:adm /var/lib/gps /var/log/gps /etc/gps
        chmod -R 750      /var/lib/gps /var/log/gps /etc/gps
    ;;

    abort-upgrade|abort-remove|abort-deconfigure)
    ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
